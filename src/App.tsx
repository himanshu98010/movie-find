import { MovieCard } from "./commponents/MovieCard";

const App = () => {
  return (
    <div className=" bg-black flex">
      <MovieCard />
      <MovieCard />
      <MovieCard />
      <MovieCard />
      <MovieCard />
    </div>
  );
};

export default App;
